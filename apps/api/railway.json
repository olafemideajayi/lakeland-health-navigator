{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema=packages/db/prisma/schema.prisma && cd apps/api && npx tsc --project tsconfig.json"
  },
  "deploy": {
    "startCommand": "cd apps/api && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
